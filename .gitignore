__pycache__/
*.py[cod]
# Metadata que deja `pip install -e .`
*.egg-info/
.venv/
venv/
.env
# Todo data/ queda fuera, sin excepciones. Listar archivo por archivo dejaba
# pasar cualquier variante (un tokens.json.viejo, un respaldo, un .bak) y ahi
# adentro viven el refresh_token de Strava y el historico.
data/

logs/
.pytest_cache/

# Copias de data/. Se generan con `python -m sport_report.respaldo`; ahi
# adentro va el mismo refresh_token y el mismo historico que en data/.
respaldos/

# Paquete de despliegue, se genera con git archive
sport_report.tgz

# Auditoria interna: notas de trabajo, no parte del proyecto.
AUDITORIA.md
